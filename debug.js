const solve = require('ndarray-linear-solve');
const ndarray = require('ndarray');
const show = require('ndarray-show');
const math = require('mathjs');


console.log('---------');
console.log(180 / Math.PI * math.acos(math.cos(330 / 180 * Math.PI)));
console.log(180 / Math.PI * math.acos(math.cos(30 / 180 * Math.PI)));
